# 검증 기록 — 2026-09-05

## 시세 모델 개선 전후: 실제 데이터

Yahoo Finance 시세를 2026-09-04까지 수집한 스냅샷으로 실행했다. 기본 시세 특징 54개, 외부 평가 12개 구간, 평가 날짜 1,362일이다. 다음 결과는 **월별 경기·수출 지표를 추가하기 전**의 시간순 모델 선택/확률 보정 변경에 대한 검증이다.

| 지표 | 기존 고정설정 앙상블 | 개선 앙상블 | 개선 − 기존, 95% 월 블록 CI |
| --- | --- | --- | --- |
| Accuracy | 0.449339 | 0.453010 | 0.003671 [−0.014463, 0.023872] |
| Balanced accuracy | 0.442783 | 0.434909 | −0.007873 [−0.025884, 0.010922] |
| Log loss | 1.030711 | 1.030247 | −0.000463 [−0.008706, 0.007313] |

정확도 차이는 약 +0.37%p지만 신뢰구간이 0을 포함한다. 균형 정확도는 낮아졌다. 따라서 통계적으로 확인된 정확도 향상으로 주장하지 않는다. 이 관측 결과를 이용해 외부 평가 구간에 맞춰 모델을 다시 선택하지 않았다.

실행 당시 시세 스냅샷 해시: `65fbf64cd83237ee29a7`. 로컬 검증 로그/산출물은 버전 관리에서 제외된 `runs/real_validation/`, `runs/real_validation.log`에 있다. 실제 Google Drive 인증/마운트는 로컬에서 실행하지 않았다.

## 월별 지표 연결

공식 KOSIS 통계표와 API 요청 형식은 확인했다. 공개 화면의 자동 조회는 차단되었으며, 이 환경에는 본인 KOSIS API 키가 없다. 따라서 **공식 API의 실제 수치 다운로드와 두 지표의 실제 정확도 효과는 아직 검증하지 못했다.** API 응답 선택·단위 변환·인증 오류 처리는 모의 응답으로 검증했다.

전체 노트북 연결 검증에는 실제 시세 캐시와 명시적으로 만든 합성 월별 자료를 사용했다. 특징 61개(기존 54개 + 월별 7개), 최근 외부 평가 3개 구간을 끝까지 실행했다. 이 실행의 정확도 수치는 경제적 효과의 근거로 사용하지 않는다.

- 지표 제외 앙상블을 포함한 방향 예측 4개와 가격 예측 3개, 총 7개 원장 행이 저장된다.
- `macro_comparison.csv`, `macro_monthly.csv`, `macro_daily_features.csv`, 비교 모델 파일과 스냅샷 해시가 저장된다.
- 마지막 시세가 2026-09-04일 때 5거래일 뒤 목표일이 2026-09-11로 계산된다.
- 기존 원장 예측의 실제값/오차 갱신, 동일 실행 중복 방지, 최초 사전 예측만 집계하는 동작은 회귀 테스트로 확인했다.

61개 단위/동작/노트북 구조 테스트 통과. `git diff --check` 통과.

```sh
python -m unittest discover -s tests
python tools/run_notebook.py --storage ./outputs --no-macro
# 본인 KOSIS_API_KEY 또는 공식 CSV 설정 후 실제 월별 자료로 실행:
python tools/run_notebook.py --storage ./outputs
```

최신 수정치의 월+2 정렬은 실제 과거 발표 자료를 재현하지 않는다. 앞으로 저장되는 사전 예측과 실제값의 비교로 지표 추가 효과를 계속 평가해야 한다.
