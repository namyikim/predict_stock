# M03 — 규제 강도 × 학습 창 내부 선택 (2026-09-10)

실행: `20260910T082518Z_samsung_m03`, `20260910T082535Z_sk_hynix_m03` (full, 고정 입력 캐시). 특징 = M02에서 고정한 current_full.
후보 6개 = Ridge alpha {100, 1000, 10000} × 학습 창 {5년, expanding}. 현행 = alpha 10000 · expanding.
선택 = 폴드마다 내부 3구간 평균 raw MAE 최소(동률이면 alpha 큰 쪽 → expanding). 발행 판정 = 내부 3구간 중 2구간 이상에서
보정(내부 기울기) 예측이 현재가 유지보다 MAE 가 낮을 때만 발행, 아니면 현재가. 평가 = M01 개발 폴드 9개 공통 행(1,081~1,086).

## 결과 (개발 폴드, 수익률 MAE, 월 블록 95% CI)

inner_selected 발행 중심값 − 현행 발행 중심값:

| 조합 | delta [CI] | 폴드별 선택 | 발행률 선택 / 현행 |
| --- | --- | --- | --- |
| samsung/5 | +0.00003 [−0.00000, +0.00007] | a10000_expanding 5 · a10000_5y 4 | 0.67 / 0.67 |
| samsung/20 | −0.00003 [−0.00009, +0.00002] | a10000_5y 4 · a10000_expanding 3 · a1000 2 | 0.44 / 0.33 |
| sk_hynix/5 | +0.00005 [−0.00015, +0.00025] | a10000_expanding 5 · a10000_5y 4 | 0.44 / 0.56 |
| sk_hynix/20 | +0.00004 [−0.00025, +0.00029] | a10000_expanding 5 · a10000_5y 3 · a1000_expanding 1 | 0.44 / 0.44 |

네 조합 모두 동률. **현행 설정(alpha=1e4, expanding) 유지.**

고정 후보별로 보면:
- raw 예측 크기는 alpha 가 작을수록 커진다(samsung/5 평균 |raw| 0.0045 → 0.016). raw MAE 는 그만큼 나빠진다(a100 이 유지보다 20~50% 큼).
  raw 가 커졌다는 것을 채택 근거로 쓰지 않는다.
- samsung/20 에서 a100·a1000 후보는 보정 후 현행보다 유의하게 낫다(a1000_expanding −0.00074 [−0.00131, −0.00027]). 그러나 내부 기울기가
  0.0007~0.007 로 사실상 0 이라 '현재가 유지'와 같은 값이다(유지 대비 delta 0.00000). 즉 우위의 정체는 "삼성 20일 현행 모델이 유지보다
  나쁘다(+0.00074 [+0.00027, +0.00131], 발행 중심값도 +0.00038 [+0.00003, +0.00081])"이지 작은 alpha 의 신호가 아니다. 내부 선택은
  raw MAE 기준이라 이 후보들을 고르지 않았다(계획대로 외부 점수로 alpha 를 고르지 않는다).
- 5년 창은 expanding 보다 250행쯤 적은 행을 실제로 쓴다(`window_rows_differ` True, 테스트로도 확인). 두 창의 차이는 모든 조합에서 동률.
- 학습 시간: 설정당 폴드당 0.011~0.014초. 비용은 고려 대상이 아니다.

## 판정과 다음 단계

- 채택 없음. 규제 강도·학습 창으로 얻을 것이 없다.
- 삼성 20일은 현행 발행 판정(내부 2/3 승)으로도 발행 중심값이 유지보다 유의하게 나쁘다 → M05 보류 정책에서 20일 지평의 발행 기준을
  더 엄격하게 할 근거. 하이닉스 5일은 보정 후 유지 대비 −0.0003 [−0.0009, +0.0003] 로 동률(M00 평가 구간의 우위는 개발 폴드에서
  재현되지 않는다 — 가설 약화).
- 다음: M04 방향 확률 모델(같은 폴드), M05 구간·보류.

## 재현

```powershell
$env:PREDICT_STOCK_PUBLISH = 'false'
python tools/run_medium_horizon.py --task M03 --target samsung --mode full --resume
python tools/run_medium_horizon.py --task M03 --target sk_hynix --mode full --resume
```
