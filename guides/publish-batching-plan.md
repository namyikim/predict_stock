# 발행 커밋 묶기 계획 — 실행 한 번 = 커밋 한 번

상태: 계획. 구현 전. 사용자가 토큰 상황을 보며 단계별로 진행한다(2026-09-23).

## 문제

- 하루 커밋 400~630개(2026-09-20~22 실측), 대부분 파일 1개짜리. 전체 이력 9,232개.
- Pages 빌드가 3일간 84회 취소, 9회만 성공 — 커밋마다 빌드가 시작되고 다음 커밋이 앞 빌드를 취소한다. 보고서 반영이 늦어진다.
- push 경합이 잦다(워크플로끼리, 사람 push 와).

원인: `tools/github_pages.publish()` 가 Contents API 로 **파일 하나 = 커밋 하나**를 만든다. 10개 도구가 60여 곳에서 호출하고, 노트북 `github_put()`(8곳)과 `build_trends/ai_news/interest_report.py` 의 `publish()` 사본도 같은 방식이다. 실행 한 번에 파일 5~17개 → 커밋 5~17개.

## 목표

1차 목표는 **도구 하나의 실행 = 변경이 있을 때 커밋 한 번**이다. 기존 호출부는 가능한 한 유지한다. 서로 다른 Python 프로세스가 실행하는 전체 워크플로를 한 커밋으로 묶는 작업은 별도 범위로 둔다.

## 설계

1. **`github_pages.Batch`** — `publish()` 와 같은 인자를 모았다가 `commit()` 때 Git Data API 로 한 번에 올린다.
   - `GET ref` → 파일마다 `POST blobs` → `POST trees`(base_tree = 현재 트리) → `POST commits` → `PATCH ref`.
   - `force:false`로 ref를 갱신한다. 경합이 확인된 경우에만 최신 ref 기준으로 최대 4회 재시도한다. 같은 파일이 바뀌었으면 최신 내용을 다시 읽어 작업을 재적용하거나 안전하게 중단한다. 상세 충돌 정책은 아래 검토 보완 사항을 따른다.
   - 같은 경로를 두 번 넣으면 마지막 것만 쓴다.
2. **점진 이행** — `with github_pages.batch("report: …"):` 블록 안의 `publish(...)` 는 컨텍스트 변수로 배치에 들어가고, 블록 밖 호출은 **지금처럼 즉시 커밋**한다. 한 번에 다 바꾸지 않아도 된다.
3. **예외 시 커밋하지 않는다** — 블록을 예외로 빠져나가면 아무것도 올리지 않는다. 반쯤 만든 보고서가 올라가지 않고, "실패하면 지난 조각 유지" 원칙과 같다.
4. **원장 안전** — 최초 읽기 시 경로별 blob SHA를 기록하고 같은 파일의 동시 변경을 감지한다. 생성한 불변 커밋의 트리에서 파일별 blob SHA를 검증한다. 이 검증만으로 기록 유실을 방지할 수 없으므로 원장은 별도의 병합 정책이 필요하다. 원장(`forecast_history/…`)만 단독 커밋할지는 노트북 단계(④)에서 정한다.
5. **Pages 재빌드 요청 유지** — `GITHUB_TOKEN`으로 만든 커밋은 Pages 자동 빌드를 유발하지 않으므로 `monthly-longterm.yml`의 명시적 `POST pages/builds`를 유지한다. 최종 발행 성공 후 한 번 요청한다. 대체 배포 경로를 실제로 검증한 뒤에만 제거한다.

## 이행 순서

| 단계 | 대상 | 비고 |
| --- | --- | --- |
| ① | `github_pages.Batch` + 영업이익·장기 전망·장기 전망 탭(`build_earnings_forecast`, `build_longterm_report`, `refresh_longterm_tab`) | 호출이 가장 많다(17+7+5). Pages 재빌드 유지 및 실제 웹 반영 확인 |
| ② | 금·은·중국·오후 갱신·세션 리뷰 | 보고서 HTML 을 읽고 고치는 도구는 `expected_sha`+`merge` |
| ③ | `build_trends/ai_news/interest_report.py` 의 `publish()` 사본을 `github_pages` 로 통합 | 중복 코드 제거 |
| ④ | 노트북 `github_put` | 원장 기록 경로 — 가장 조심. 별도 세션 |

각 단계 완료 시 이 문서에 체크하고 커밋 수(전후 하루 커밋 수, Pages 취소 비율)를 적는다.

- [x] ① Batch 구현·테스트, 영업이익·장기 전망·탭 갱신 이행, Pages 재빌드 유지 — 2026-09-24. **웹 반영 확인은 다음 실행(매일 10:17 KST) 뒤** 아래 '①단계 기록'에 적는다
- [x] ② 금·은·중국·오후 갱신·세션 리뷰 이행 — 2026-09-24. 실행 한 번 = 커밋 한 번(금속 7→1, 오후 갱신 4~6→1,
  세션 리뷰 2~4→1, 중국 1→1·내용 그대로면 0). 함께 고친 것:
  - 금속 원장: 처음 읽은 sha 로만 올린다(`fetch_with_sha` → `expected_sha`, 합치기 없음 — 바뀌었으면 묶음 전체를 멈춘다).
  - 오후 갱신·세션 리뷰의 보고서 HTML: 노트북도 같은 파일을 다시 쓰므로 `expected_sha` + 최신본에 절만 다시 넣는
    `merge`. 예전에는 읽은 뒤 노트북이 새로 올린 보고서를 옛 본문으로 덮어쓸 수 있었다.
  - `Batch` 가 **함수로 준 내용**을 커밋할 때 만든다. 오후 갱신의 파생 파일(일별 비교·요약)은 원장이 합쳐지면
    다시 계산되므로, 미리 읽어 두면 합치기 전 원장 기준이 올라갔다. 원장을 먼저 넣고 파생 파일은 함수로 넣는다.
- [x] ③ `publish()` 사본 통합 — 2026-09-24. 사본은 이미 `github_pages.publish` 에 위임만 하고 있었다(재시도 없는 PUT
  사본은 2026-09-12 금리 보고서 사고 뒤 정리됨). 남은 위임 함수를 지우고 공용 발행기를 직접 부르며, 묶음으로 올린다:
  검색어·관심도 보고서+날짜별 보관본 2→1커밋, AI 뉴스 1→1(내용이 그대로면 0).
- [x] ④ 노트북 `github_put` 이행 — 2026-09-24. **원장은 보고서와 따로 커밋한다**(보고서 발행이 실패해도 원장은 남아야
  한다 — 설계 4의 결정). 실행 한 번(종목 하나): 원장 폴더 4~5 + 보고서 2 = 6~7커밋 → 2(참고자료 보관본이 바뀐 날 3).
  - 노트북 자체 발행 코드를 없앴다. `tools/sync_notebook_helpers.py` 가 `tools/github_pages.py` 를 그대로 헬퍼 셀
    (태그 `github_pages`)에 넣어 모듈로 실행한다 — Colab 에서도 자립하고, 도구와 발행 코드가 한 벌이다.
    `github_put`·`github_put_expected` 는 `github_pages.publish` 로 위임하는 두 줄짜리가 됐다.
  - 원장 폴더: 원장(처음 읽은 sha, 바뀌었으면 `merge_ledger_logs` 로 합침) → 파생 파일은 함수로(합친 원장 기준) → 격자.
  - 참고자료 보관본은 한 커밋으로 묶고, 커밋이 실패해도 실행을 멈추지 않는다(예전과 같다).
  - 실험 기록 셀(트랜스포머, 드물게 실행)은 그대로 둔다 — 공용 발행기를 쓰므로 안전하고 커밋 수 몫이 작다.
- [ ] 효과 측정: 이행 전후 하루 커밋 수, Pages 빌드 성공/취소 비율

## 테스트

가짜 API(urlopen 대체)로:
- 여러 파일 → blob N 개, tree 1, commit 1, ref 1 호출.
- ref 경합(422) → 최신 base 로 재구성·재시도, 앞 시도의 부분 반영 없음.
- 블록 밖 `publish()` 는 기존처럼 즉시 커밋(회귀).
- 예외로 블록을 빠져나가면 커밋하지 않음.
- 커밋 후 blob sha 검증이 불일치를 잡음.
- 토큰 문자열이 로그·예외 메시지에 나오지 않음.

## 기대 효과와 위험

- 커밋 하루 400~600 → 60~100 개 추정. Pages 취소 대부분 제거. push 경합 감소.
- 요청 수는 비슷하다(파일당 blob 요청). GITHUB_TOKEN 레이트 리밋(시간당 1,000) 안쪽.
- 배치가 한 번 실패하면 그 실행의 파일이 전부 안 올라간다 → 지난 보고서가 그대로 남는다(현재 원칙과 같음).


## 검토 보완 사항 — 2026-09-23

상태: 문서 검토 반영. 아래 항목은 구현 완료를 뜻하지 않는다. 기존 계획과 충돌하는 경우 이 절의 안전 조건을 우선한다.

### 필수 안전 조건

1. **같은 파일의 동시 변경을 보호한다.** 최신 base_tree를 사용하는 것만으로는 충분하지 않다. 두 실행이 같은 원장을 읽은 뒤 각각 기록을 추가하면, 늦게 저장하는 실행의 오래된 전체 내용이 먼저 추가된 기록을 덮어쓸 수 있다. 최초 읽기 시점의 경로별 blob SHA와 최신 SHA를 비교한다. 다른 파일만 바뀐 경우에는 재시도하고, 같은 파일이 바뀐 경우에는 최신 원장에 안정적인 레코드 키로 병합하거나 안전하게 중단한다. 저장 후 해시 일치는 오래된 내용의 덮어쓰기까지 성공으로 판정하므로 병합 정책을 대신하지 못한다.
2. **HTML 부분 갱신을 보존한다.** `refresh_longterm_tab.publish_tab()`은 충돌할 때 최신 HTML을 다시 읽고 장기 전망 영역만 재적용한다. 배치 이행 후에도 이 동작을 유지한다. 해당 함수는 공통 `publish()` 대신 직접 API를 호출하므로 컨텍스트만 추가해서는 배치에 포함되지 않는다.
3. **Pages 배포를 유지한다.** 현재 토큰과 발행 방식에서는 커밋 감소가 자동 배포를 보장하지 않는다. 최종 발행 뒤 명시적인 빌드 요청을 유지하고 실제 웹 보고서 갱신까지 확인한다.
4. **검증 대상은 생성한 커밋으로 고정한다.** 커밋 후 최신 ref에서만 내용을 비교하면 다른 실행의 정상적인 후속 변경도 오류로 볼 수 있다. 생성한 커밋의 트리를 검증하고, 필요하면 현재 브랜치에 해당 커밋이 포함되어 있는지 별도로 확인한다.

### 구현 범위와 성공 처리

- `ContextVar`는 프로세스 간에 공유되지 않는다. 현재 실적 전망 생성·장기 전망 생성·탭 갱신은 별도 명령이다. 1차 목표는 도구별 배치이며, 전체 워크플로의 원자적 발행이 필요하면 결과물과 manifest를 모은 뒤 마지막 단계에서 일괄 발행하는 구조를 별도로 설계한다.
- 기존 `publish()`는 문서 설명과 달리 content blob SHA 앞 7자를 반환한다. 배치 대기 중에는 최종 커밋 SHA나 성공을 반환할 수 없으므로 반환값 계약과 호출부 로그를 명확히 한다. 대기 상태는 '발행 대기', 최종 ref 갱신·검증 성공 후에만 '발행 완료'로 표시한다.
- 블록 밖으로 전파된 예외만 감지해서는 내부에서 잡고 넘긴 오류를 알 수 없다. 필수 결과물과 선택적 캐시를 구분하고, 필수 파일 생성 실패 시 배치를 중단하는 정책을 명시한다.
- 동일한 blob은 제외한다. 변경 파일이 없는 배치는 커밋과 Pages 요청을 생략한다.
- 409/422를 모두 경합으로 간주하지 않는다. 최신 HEAD와 오류 응답으로 재시도 가능한 경합을 확인하고, 유효성 검사·권한 오류는 실패 처리한다.
- ref 갱신 응답이 유실된 경우 서버에서는 이미 반영됐을 수 있다. 재시도 전 생성한 커밋의 반영 여부를 확인해 중복 커밋을 방지한다.

### 추가 검증 및 완료 체크

- [x] 같은 원장에 서로 다른 기록을 동시에 추가해도 기록이 모두 보존된다. — 합칠 수 있는 파일은 merge, 없으면 묶음 전체 중단(`test_publish_batch`). 원장 저장 자체는 799f52bf·41002f57
- [x] 동일 HTML의 서로 다른 영역을 동시에 수정해도 두 변경이 보존된다. — `publish_tab` 이 공용 publish(expected_sha, merge=장기 전망 영역 재적용)로 바뀌어 묶음 안에서도 같다(`test_export_refresh`)
- [x] 관련 없는 파일의 변경은 보존하면서 재시도한다. — ref 갱신이 거절되면 최신 main 을 base_tree 로 다시 짠다
- [x] 변경 없는 배치는 커밋·배포 요청을 생성하지 않는다. — 같은 blob 은 건너뛰고, 커밋이 생길 때만 `PAGES_CHANGED_FLAG` 표시 파일을 남겨 워크플로가 Pages 재빌드를 요청한다
- [x] 필수 파일 생성 실패 및 내부에서 처리된 예외의 정책을 검증한다. — 블록 안에서 예외가 나면 아무것도 올리지 않는다. 도구 안의 보조 자료 try/except 는 '모아 두기'만 감싸므로(실패할 일이 없다) 정책이 바뀌지 않는다. 대신 한 묶음이 실패하면 보조 자료 사본도 함께 안 올라간다 — 예전에는 따로 올라갔다
- [x] ref 갱신 성공 뒤 응답 유실 시 중복 커밋 없이 복구한다. — compare API 로 만든 커밋이 main 에 들어갔는지 확인한다
- [x] 후속 커밋이 생겨도 생성한 커밋의 검증이 잘못 실패하지 않는다. — 검증은 최신 main 이 아니라 만든 커밋의 트리 sha 로 한다. blob 은 로컬에서 계산한 sha 와 서버가 준 sha 를 대조한다
- [x] 기존 블록 밖 호출의 반환값·로그 호환성을 확인한다. — 블록 밖은 그대로(blob sha 7자), 블록 안은 '대기(묶음 발행)'. `publish_tab` 반환값은 커밋 sha 에서 blob sha 로 바뀌었다(출력용이라 영향 없음)
- [ ] 최종 발행 후 실제 Pages 화면 갱신을 확인한다.

### 효과 측정의 해석

기존 문서의 실행·커밋 집계는 작성자가 제시한 수치이며 이번 검토에서 독립적으로 재집계하지 않았다. 하루 60~100개 커밋과 Pages 취소 감소는 목표 추정치다. 여러 워크플로 간 배포 경합은 남을 수 있으므로 배포 취소 원인, 도구별 커밋 수, API 요청 수, 실제 웹 반영 시간을 전후 비교한다. API 한도 충족도 토큰 종류와 동시 실행량을 포함해 확인한다.

### 근거

- [GitHub Pages 발행 소스 설정 — GITHUB_TOKEN 커밋의 빌드 제한](https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site)
- [Git references API — force, 409/422 응답](https://docs.github.com/en/rest/git/refs)
- 검토 코드: `tools/github_pages.py`, `tools/refresh_longterm_tab.py`, `.github/workflows/monthly-longterm.yml`

## Claude 재검토 — 2026-09-23

위 "검토 보완 사항"(GPT)을 코드와 GitHub API 실측으로 확인했다. 상태: 문서 검토. 구현 아님.

### 항목별 판정

| 지적 | 판정 | 확인 근거 |
| --- | --- | --- |
| 같은 파일 동시 기록 시 유실 | 맞음 — **현재 코드에 이미 있다** | 아래 '기존 유실 위험' |
| `publish_tab()` 은 공통 `publish()` 대신 직접 API | 맞음 | `refresh_longterm_tab.py` 가 `github_pages._api` 를 직접 호출하고 409 때 최신 HTML 에 재적용한다 |
| `publish()` 반환값은 blob SHA 7자 | 맞음 | `["content"]["sha"][:7]`. 독스트링의 "커밋 sha" 는 틀린 설명이다 |
| `ContextVar` 는 프로세스 간 공유되지 않는다 | 맞음 | 원래 계획의 "실행 한 번 = 커밋 한 번" 은 과장이다. 정확히는 **도구 한 번 = 커밋 한 번**. 하루 60~100개 추정도 낙관적이며 이행 후 실측으로 다시 정한다 |
| 생성한 커밋의 트리로 검증 | 맞음 | 최신 ref 로 비교하면 다른 실행의 정상 후속 커밋을 오류로 본다 |
| 409/422 구분, ref 갱신 응답 유실 시 중복 커밋 방지, 무변경 배치 생략 | 맞음 | 표준적인 보완 |
| GITHUB_TOKEN 커밋은 Pages 자동 빌드를 일으키지 않는다 | **이 저장소 실측과 다르다** | 아래 'Pages 실측' |

### Pages 실측

`GET /repos/.../pages/builds` 최근 100건(2026-09-23 04:53~06:25 UTC, 92분):
- 빌드 촉발자: `github-actions[bot]` 97건, `namyikim` 3건.
- 빌드가 붙은 서로 다른 커밋 79개. 커밋당 빌드 1회 58개, 2회 21개(자동 + 명시적 요청으로 보인다).
- 상태: built 9, errored 81(대부분 뒤 빌드에 밀린 것), building 10.
- 명시적 `POST pages/builds` 는 daily-report 6곳, afternoon-report 1곳, monthly-longterm 1곳 — 실행당 최대 6~7회라 92분에 79개 커밋의 빌드를 설명하지 못한다.

→ 이 저장소에서는 **봇 커밋이 실제로 Pages 빌드를 일으킨다.** GPT 의 이유는 맞지 않지만 "명시적 요청을 대체 경로 검증 전까지 유지" 라는 결론은 해가 없으므로 그대로 따른다. 커밋을 줄이면 Pages 취소가 줄어든다는 원래 기대도 이 실측과 일치한다.

### 기존 유실 위험 (배치와 무관하게 지금 있다)

- `github_pages.publish()` 와 노트북 `github_put()` 은 409/422 때 sha 만 다시 읽고 **자기 전체 내용으로 다시 덮어쓴다.** 독스트링은 "파일 전체를 덮어쓰므로 재시도가 안전하다" 고 하지만, 다른 실행이 그 사이 행을 추가했다면 그 행이 지워진다.
- `forecast_history/<target>/forecast_log.csv`(와 daily_forecast_comparison·forecast_accuracy_summary)를 **오후 갱신**(`build_afternoon_update.py`, concurrency 그룹 `afternoon-report`)과 **일일 보고서 노트북**(그룹 `daily-report`)이 둘 다 쓴다. 그룹이 달라 동시에 돌 수 있다.
- 두 실행이 겹치면 늦게 쓴 쪽이 먼저 추가된 기록을 지운다. 시간대(오후 갱신 09:37·16:10 KST, 일일 기록 06~09·17~23 KST)가 대체로 어긋나 확률은 낮지만, 원장은 이 저장소에서 가장 중요한 자산이다.

### 제안 (결정 대기)

1. **원장 유실 방지를 배치와 별개로 먼저.** 가장 간단한 대책: 원장을 쓰는 워크플로(일일 보고서·오후 갱신·채점)를 같은 concurrency 그룹으로 묶는다. 워크플로당 한 줄이며 실행이 서로 기다리게 되는 것이 비용이다. 근본 대책(충돌 시 최신 원장을 다시 읽어 레코드 키로 병합)은 배치 ④단계에서 한다.
2. `publish()` 독스트링을 실제 반환값(blob SHA)과 재시도의 한계(전체 덮어쓰기)에 맞게 고친다.
3. 효과 목표는 "도구 단위 커밋"으로 다시 적고, 수치는 ① 이행 후 실측으로 정한다.

- [x] 제안 1 — **택하지 않음.** 그룹의 대기 자리는 하나라 일일 보고서의 잦은 재시도가 09:37 채점을 취소할 수 있다. 대신 처음 읽은 sha 로만 저장하고 바뀌었으면 합친다(799f52bf 오후 갱신, 41002f57 노트북)
- [x] 제안 2 — `publish()` 독스트링 정정(799f52bf)


## ①단계 기록 — 2026-09-24

구현: `tools/github_pages.py` 의 `batch()`·`Batch`·`blob_sha`. 대상 세 도구의 발행 구간을 `with github_pages.batch(...)` 로
감쌌다(들여쓰기만 바뀜). 도구마다 프로세스가 따로라 **도구 한 번 = 커밋 한 번**이다(원래 계획의 "실행 한 번"이 아님).

| 도구 | 이전 커밋 수(한 번 실행) | 이후 |
| --- | --- | --- |
| `build_earnings_forecast` | 보조 자료 0~7 + 원장 1 + 조각 3 | 1 (바뀐 것이 없으면 0) |
| `build_longterm_report` | 보조 자료 0~4 + 조각 2 | 1 (0) |
| `refresh_longterm_tab` | 1 | 1 (0) |

예상: 워크플로 한 번(두 종목) 약 12~30 커밋 → 최대 6. 하루 전체 커밋 중 이 워크플로 몫은 작다 — 큰 몫은 일일 보고서
노트북(④)과 오후 갱신·검색어·뉴스(②·③)다. 효과는 ②~④ 뒤에 함께 잰다.

읽고-고치고-쓰는 파일의 규칙(안전 조건 1):

| 파일 | 규칙 |
| --- | --- |
| `forecast_history/<종목>/earnings_log.csv` | 처음 읽은 sha 로만. 바뀌었으면 **합치지 않고 묶음 전체를 멈춘다**(같은 원장을 쓰는 실행이 동시 실행 그룹·`max-parallel: 1` 로 이미 막혀 있어 합치기 규칙까지 만들 필요가 없다) |
| `macro_history/cli_g20_vintages.csv` | 두 종목 실행이 같은 파일에 덧붙인다. 바뀌었으면 최신 판본에 다시 덧붙인다 |
| `docs/<종목>/index.html` | 장기 전망 영역만 교체. 바뀌었으면 최신 페이지에 다시 적용한다 |

### 실제 실행 확인 — 2026-09-24 10:16~10:28 KST (Actions run 35942173879, push 트리거, 두 종목 성공)

| 종목 | 커밋 | 파일 |
| --- | --- | --- |
| samsung | `de7ad64d` earnings | earnings.html · earnings.json · exports_snapshot.json · macro_history/cli_g20.csv (원장은 그대로라 빠짐) |
| samsung | `281e1726` longterm | longterm.html · longterm.json · valuation_history/… · macro_history/cli_g20.csv |
| samsung | `c42c5dcb` 탭 갱신 | index.html |
| sk_hynix | `ea71f245` · `84c29c20` · `c1cc2d8b` | 각 4 · 4 · 1 |

종목당 3커밋(예전 방식이면 9~10). Pages 화면의 `earnings.json` 생성 시각이 이번 실행(10:18·10:26 KST)으로 바뀐 것을 확인했다.

### 이 실행에서 드러난 별개 문제 — 보관본 핑퐁(묶기 이전부터, 2026-09-21~)

같은 보관본을 두 도구가 서로 다른 기간으로 받아 **통째로 덮어써** 매 실행 두 번씩 바뀐다. 묶기와 무관하게 쓸데없는
커밋이 생기고, 마지막에 짧은 쪽이 쓰면 오래된 이력이 빠진 채 남는다.

| 파일 | 긴 쪽 | 짧은 쪽 | 매번 |
| --- | --- | --- | --- |
| `macro_history/cli_g20.csv` | 장기 전망(1998-01~) | 영업이익(2000-01~) | 24행 삭제 → 복구 |
| `macro_history/news_sentiment.csv` | 장기 전망(2005-01~, 7,934행) | 일일 보고서 노트북(2014-07~, 4,466행) | 3,468행 삭제 → 복구. 2026-09-24 10:27 현재 짧은 쪽 상태 |

대책 후보: 공용 이력 보관본은 덮어쓰지 않고 기존 파일과 날짜로 합친다(겹치는 날짜는 새 값). 같은 원천이라 겹치는
값은 같다.
